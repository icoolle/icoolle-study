package ${packageName}.model.${moduleName}.dto;

import io.swagger.annotations.ApiModel;
import io.swagger.annotations.ApiModelProperty;
import lombok.Data;
import lombok.experimental.Accessors;

import javax.validation.constraints.NotNull;

/**
* @author: ${author}
* @description: ${comment}修改实体参数
* @projectName: ${className}UpdateDTO
* @package: ${packageName}.model.${moduleName}.dto
* @date: ${nowDate}
* @version: V1.0.0
*/
@Data
@ApiModel("${comment}修改实体参数")
@Accessors(chain = true)
public class ${className}UpdateDTO extends ${className}SaveDTO {

    @NotNull(message = "主键ID不能为空值")
    @ApiModelProperty("主键ID")
    private Long id;
}
